version: '3'

services:
  postgres:
    image: postgres:latest
    container_name: nest-postgres

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: mydb

    ports:
      - "5432:5432"
